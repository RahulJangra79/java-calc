# name: Java Calculator CI

# on:
#   push:
#     branches: [ "main" ]
#   pull_request:
#     branches: [ "main" ]

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Java
#         uses: actions/setup-java@v4
#         with:
#           distribution: 'temurin'
#           java-version: '17'

#       - name: Compile Java Program
#         run: javac src/Calculator.java

#       - name: Run Java Program (Smoke Test)
#         run: |
#           echo -e "+\n5\n3" | java -cp src Calculator


name: Java Calculator CI with Chained Testing

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Compile Calculator
        run: javac src/Calculator.java

      - name: Download JUnit
        run: |
          curl -L -o junit-platform-console-standalone.jar \
          https://repo1.maven.org/maven2/org/junit/platform/junit-platform-console-standalone/1.10.1/junit-platform-console-standalone-1.10.1.jar

      - name: Compile Tests
        run: javac -cp .:junit-platform-console-standalone.jar src/CalculatorTest.java

      - name: Run Chained Tests
        run: |
          java -jar junit-platform-console-standalone.jar \
          --class-path src \
          --scan-class-path
